---
created: 2026-08-15
source: implicit-extraction:Data Modeling
note_type: entity
tags: [entity, window-function, Data Modeling]
---

# ROW_NUMBER() Window Function

<!-- SQL window function used for deduplication: PARTITION BY item groups rows, ORDER BY fromdate sequences them, and WHERE seqnum=1 picks one row per group. -->

## Definition

SQL window function used for deduplication: PARTITION BY item groups rows, ORDER BY fromdate sequences them, and WHERE seqnum=1 picks one row per group.

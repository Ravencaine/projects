---
created: 2026-08-15
source: implicit-extraction:Data Modeling
note_type: entity
tags: [entity, dimensional-modeling, Data Modeling]
---

# Snowflake Schema

<!-- A normalised dimensional schema where dimension tables are split into normalised sub-tables (Country, Region, District, Store). Adds multi-hop joins that degrade Power BI refresh and query performance. -->

## Definition

A normalised dimensional schema where dimension tables are split into normalised sub-tables (Country, Region, District, Store). Adds multi-hop joins that degrade Power BI refresh and query performance.

-r requirements.txt

# Test runner
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<0.29  # also a runtime dep; pinned here for tests

# Linting / formatting
ruff>=0.5,<1.0
mypy>=1.10,<2.0
types-requests

# Type stubs
pydantic[email]

# Build / packaging
pyinstaller>=6.10,<7.0
---
created: 2026-08-15
source: implicit-extraction:Power BI
note_type: claim
tags: [claim, pattern, Power BI]
---

# Card geometry as data tuples enables scalable dashboard maintenance without component code changes

<!-- Defining card content as a list of tuples (card_id, title, value, subtitle, back_fig, front_fig) and rendering via list comprehension means adding, removing, or reordering cards requires only a change to the data. Component definitions remain untouched. -->

## Claim

Defining card content as a list of tuples (card_id, title, value, subtitle, back_fig, front_fig) and rendering via list comprehension means adding, removing, or reordering cards requires only a change to the data. Component definitions remain untouched.

## Evidence

- Stated in [[Data-Driven-UI-Card-Tuples]] — Purpose

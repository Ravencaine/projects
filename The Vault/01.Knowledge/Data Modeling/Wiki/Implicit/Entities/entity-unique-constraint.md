---
created: 2026-08-15
source: implicit-extraction:Data Modeling
note_type: entity
tags: [entity, constraint, Data Modeling]
---

# UNIQUE Constraint

<!-- Postgres constraint that prevents duplicate values in a column, catching duplicates that Excel silently allows. -->

## Definition

Postgres constraint that prevents duplicate values in a column, catching duplicates that Excel silently allows.

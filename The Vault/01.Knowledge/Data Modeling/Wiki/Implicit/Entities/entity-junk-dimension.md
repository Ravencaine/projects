---
created: 2026-08-15
source: implicit-extraction:Data Modeling
note_type: entity
tags: [entity, dimension-table, Data Modeling]
---

# Junk Dimension

<!-- A dimensional modeling pattern that combines multiple low-cardinality, related attributes into a single dimension table instead of separate tables. Use when attributes are always filtered together and none are reused across fact tables as conformed dimensions. -->

## Definition

A dimensional modeling pattern that combines multiple low-cardinality, related attributes into a single dimension table instead of separate tables. Use when attributes are always filtered together and none are reused across fact tables as conformed dimensions.

---
created: 2026-08-15
source: implicit-extraction:Data Modeling
note_type: entity
tags: [entity, postgres, Data Modeling]
---

# PostgreSQL

<!-- Relational database engine that enforces data quality rules at the engine level through constraints (UNIQUE, NOT NULL, FOREIGN KEY, CHECK, type constraints). -->

## Definition

Relational database engine that enforces data quality rules at the engine level through constraints (UNIQUE, NOT NULL, FOREIGN KEY, CHECK, type constraints).
